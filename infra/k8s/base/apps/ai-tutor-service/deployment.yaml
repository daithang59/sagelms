apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-tutor-service
  labels:
    app.kubernetes.io/name: ai-tutor-service
    app.kubernetes.io/part-of: sagelms
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: ai-tutor-service
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ai-tutor-service
        app.kubernetes.io/part-of: sagelms
    spec:
      serviceAccountName: ai-tutor-service
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ai-tutor-service
          image: harbor.sagelms.id.vn/sagelms-app/ai-tutor-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8087
          envFrom:
            - configMapRef:
                name: ai-tutor-service-config
            - secretRef:
                name: db-app-secret
            - secretRef:
                name: app-shared-secret
            - secretRef:
                name: ai-tutor-secret
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 768Mi
          securityContext:
            allowPrivilegeEscalation: false
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            capabilities:
              drop:
                - ALL
