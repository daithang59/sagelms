apiVersion: v1
kind: Service
metadata:
  name: ai-tutor-service
  labels:
    app.kubernetes.io/name: ai-tutor-service
    app.kubernetes.io/part-of: sagelms
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-tutor-service
  ports:
    - name: http
      port: 8087
      targetPort: http
