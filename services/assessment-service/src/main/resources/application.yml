server:
  port: 8085

spring:
  application:
    name: sagelms-assessment-service
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:sagelms}
    username: ${DB_USER:sagelms}
    password: ${DB_PASSWORD:sagelms}
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: assessment
  flyway:
    schemas: assessment
    baseline-on-migrate: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
